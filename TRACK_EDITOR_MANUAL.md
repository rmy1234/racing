# 트랙 에디터 사용 매뉴얼

## 목차
1. [트랙 에디터 기본 사용법](#트랙-에디터-기본-사용법)
2. [트랙 생성하기](#트랙-생성하기)
3. [트랙 편집하기](#트랙-편집하기)
4. [체크포인트 추가하기](#체크포인트-추가하기)
5. [게임에 트랙 적용하기](#게임에-트랙-적용하기)
6. [고급 기능](#고급-기능)

---

## 트랙 에디터 기본 사용법

### 접근 방법
1. 게임 홈 화면에서 **"트랙 편집기"** 버튼 클릭
2. 또는 브라우저에서 `/track-editor.html` 직접 접속

### 주요 기능
- **포인트 추가**: 트랙의 중심선을 이루는 포인트 추가
- **포인트 이동**: 기존 포인트를 드래그하여 이동
- **포인트 삭제**: 불필요한 포인트 제거
- **체크포인트 추가**: 레이스 진행을 위한 체크포인트 배치
- **미리보기**: 실제 게임에서 보이는 트랙 모습 확인

---

## 트랙 생성하기

### 1단계: 기본 정보 입력
- **트랙 이름**: 게임에서 표시될 트랙 이름 (예: "내 커스텀 트랙")
- **트랙 ID**: 코드에서 사용할 고유 식별자 (영문, 소문자, 하이픈 사용 가능, 예: `my-custom-track`)
- **맵 너비**: 트랙의 가로 크기 (픽셀 단위, 예: 4500)
- **맵 높이**: 트랙의 세로 크기 (픽셀 단위, 예: 3000)
- **트랙 폭**: 트랙의 폭 (기본값: 120)

### 2단계: 포인트 추가
1. **"➕ 포인트 추가"** 모드 선택
2. 캔버스를 클릭하여 트랙의 중심선을 따라 포인트 추가
3. 최소 2개 이상의 포인트 필요
4. 포인트를 추가하면 자동으로 직선으로 연결됨

### 3단계: 포인트 타입 설정
- **직선**: 포인트 간을 직선으로 연결
- **곡선**: 포인트 간을 부드러운 곡선으로 연결
- 포인트를 선택한 후 **"직선"** 또는 **"곡선"** 버튼으로 타입 변경

### 4단계: 트랙 경로 조정
- **"↔️ 포인트 이동"** 모드로 포인트를 드래그하여 위치 조정
- **"✖️ 포인트 삭제"** 모드로 불필요한 포인트 제거
- 포인트 목록에서 직접 좌표 입력도 가능

### 5단계: 미리보기 확인
- **"👁️ 미리보기"** 버튼을 클릭하여 실제 게임에서 보이는 트랙 모습 확인
- 미리보기 모드에서는 트랙의 실제 폭과 모양을 확인할 수 있음

---

## 트랙 편집하기

### 포인트 편집
1. 포인트 목록에서 편집할 포인트 클릭
2. 사이드바의 **"선택된 포인트"** 섹션에서 좌표 수정
3. X, Y 좌표를 직접 입력하거나 드래그로 이동

### 포인트 타입 변경
1. 포인트 선택
2. **"직선"** 또는 **"곡선"** 버튼 클릭
3. 직선과 곡선이 만나는 지점은 자동으로 부드럽게 연결됨

### 트랙 정보 수정
- 트랙 이름, ID, 맵 크기, 트랙 폭은 언제든지 수정 가능
- 수정 시 코드가 자동으로 업데이트됨

---

## 체크포인트 추가하기

### 체크포인트란?
레이스 진행을 추적하기 위한 지점입니다. 플레이어가 체크포인트를 순서대로 통과해야 합니다.

### 체크포인트 추가 방법
1. **"🏁 체크포인트 추가"** 모드 선택
2. 트랙 위의 원하는 위치 클릭
3. 체크포인트가 추가되며, 가장 가까운 트랙 포인트의 방향으로 각도가 자동 설정됨

### 체크포인트 편집
1. **"↔️ 체크포인트 이동"** 모드로 체크포인트 드래그
2. 선택된 체크포인트의 좌표와 각도 수정 가능
3. 각도는 라디안 또는 도(degree) 단위로 입력 가능

### 체크포인트 범위
- **트랙 에디터에서 선택/삭제**: 15 픽셀
- **게임에서 통과 감지**:
  - basic-circuit: 360 픽셀
  - monza: 300 픽셀
  - (트랙 크기에 따라 조정됨)

### 체크포인트 배치 팁
- 트랙을 따라 순서대로 배치
- 각 코너나 주요 지점에 배치
- 시작 라인 근처에 첫 번째 체크포인트 배치

---

## 게임에 트랙 적용하기

### 1단계: 코드 생성 및 복사
1. 트랙 편집 완료 후 사이드바의 **"생성된 코드"** 섹션 확인
2. **"코드 복사"** 버튼 클릭하여 코드를 클립보드에 복사

### 2단계: 클라이언트 측 트랙 파일 생성

#### 파일 생성
`public/js/tracks/[track-id].js` 파일을 생성합니다.
- 예: 트랙 ID가 `my-track`인 경우 → `public/js/tracks/my-track.js`

#### 파일 내용 작성
복사한 코드를 붙여넣고, 파일 끝에 다음 코드를 추가합니다:

```javascript
// 트랙 등록
registerTrack('my-track', MyTrackTrack);

// getSmoothPath 함수 추가 (부드러운 경로 생성)
MyTrackTrack.getSmoothPath = function() {
  // Catmull-Rom 스플라인으로 부드러운 경로 생성
  // 또는 단순히 centerPath 반환
  return this.centerPath;
};

// 트랙 위에 있는지 판단하는 함수
MyTrackTrack.isOnTrack = function(x, y) {
  const margin = 50; // 트랙 경계 여유 공간
  const halfWidth = this.trackWidth / 2;
  
  // centerPath에서 가장 가까운 점 찾기
  let minDist = Infinity;
  for (let i = 0; i < this.centerPath.length; i++) {
    const p = this.centerPath[i];
    const dx = x - p.x;
    const dy = y - p.y;
    const dist = Math.sqrt(dx * dx + dy * dy);
    if (dist < minDist) {
      minDist = dist;
    }
  }
  
  return minDist <= halfWidth + margin;
};

// 가장 가까운 체크포인트 찾기
MyTrackTrack.getNearestCheckpoint = function(x, y) {
  let nearestIdx = 0;
  let minDist = Infinity;
  
  for (let i = 0; i < this.checkpoints.length; i++) {
    const cp = this.checkpoints[i];
    const dx = x - cp.x;
    const dy = y - cp.y;
    const dist = Math.sqrt(dx * dx + dy * dy);
    if (dist < minDist) {
      minDist = dist;
      nearestIdx = i;
    }
  }
  
  return nearestIdx;
};

// 체크포인트 통과 확인
MyTrackTrack.checkCheckpoint = function(x, y, lastCheckpoint) {
  const checkpointRadius = 300; // 체크포인트 감지 범위
  const nextCheckpoint = (lastCheckpoint + 1) % this.checkpoints.length;
  const cp = this.checkpoints[nextCheckpoint];
  
  const dx = x - cp.x;
  const dy = y - cp.y;
  const dist = Math.sqrt(dx * dx + dy * dy);
  
  if (dist < checkpointRadius) {
    return nextCheckpoint;
  }
  
  return lastCheckpoint;
};

// 커브(연석) 데이터 (선택사항)
MyTrackTrack.curbs = [];
```

> **참고**: `isOnTrack`, `getNearestCheckpoint`, `checkCheckpoint` 함수는 기존 트랙 파일(`basic-circuit.js`, `monza.js`)을 참고하여 구현할 수 있습니다.

### 3단계: track.js에 트랙 등록

`public/js/track.js` 파일을 열고 `TRACKS` 객체에 새 트랙을 추가합니다:

```javascript
const TRACKS = {
  'basic-circuit': {
    id: 'basic-circuit',
    name: '기본 서킷',
    data: null
  },
  'monza': {
    id: 'monza',
    name: '몬차 서킷',
    data: null
  },
  // 새 트랙 추가
  'my-track': {
    id: 'my-track',
    name: '내 트랙',  // 게임에서 표시될 이름
    data: null
  }
};
```

### 4단계: 서버 측 설정 추가

`src/game/tracks/track-configs.ts` 파일을 열고 다음을 추가합니다:

#### 서버 설정 객체 생성
```typescript
// 새 트랙 서버 설정
export const myTrackServerConfig: TrackServerConfig = {
  buildCenterPath: function() {
    // 에디터에서 생성한 centerPath 배열 반환
    return [
      { x: 100, y: 100 },
      { x: 200, y: 100 },
      { x: 300, y: 200 },
      // ... 에디터에서 생성한 모든 포인트들
    ];
  },
  spawnPositions: [
    { x: 100, y: 100 },  // 첫 번째 포인트 위치
    { x: 100, y: 120 },  // 약간 옆으로 이동
    { x: 100, y: 140 },
    { x: 100, y: 160 },
  ],
  spawnAngle: 0,  // 출발 각도 (라디안)
  checkpoints: [
    { x: 500, y: 500, angle: 0 },
    { x: 1000, y: 1000, angle: Math.PI / 2 },
    // ... 에디터에서 생성한 모든 체크포인트들
  ],
  startLine: {
    x: 100,  // 첫 번째 포인트의 x 좌표
    y: 100,  // 첫 번째 포인트의 y 좌표
    angle: 0  // 시작 라인 각도
  },
  trackWidth: 120  // 트랙 폭 (모든 트랙에서 120으로 통일)
};
```

#### trackServerConfigs Map에 추가
파일 하단의 `trackServerConfigs` Map에 새 트랙을 추가합니다:

```typescript
export const trackServerConfigs: Map<string, TrackServerConfig> = new Map([
  ['basic-circuit', basicCircuitServerConfig],
  ['monza', monzaServerConfig],
  ['my-track', myTrackServerConfig],  // 추가
]);
```

### 5단계: HTML에 트랙 파일 로드 추가

`public/index.html` 파일에서 트랙 파일을 로드하는 부분을 찾아 새 트랙 파일을 추가합니다:

```html
<script src="/js/track.js"></script>
<script src="/js/tracks/basic-circuit.js"></script>
<script src="/js/tracks/monza.js"></script>
<script src="/js/tracks/my-track.js"></script>  <!-- 추가 -->
```

### 6단계: 서버 재시작

서버를 재시작하여 새 트랙 설정이 적용되도록 합니다.

```bash
# 개발 서버 재시작
npm run dev
# 또는
npm start
```

### 7단계: 게임에서 확인

1. 게임 홈 화면으로 이동
2. 트랙 선택 드롭다운에서 새로 추가한 트랙 확인
3. 트랙을 선택하고 게임 시작하여 정상 작동 확인

---

## 고급 기능

### 캔버스 조작
- **확대/축소**: 마우스 휠로 확대/축소 (마우스 커서 위치 기준)
- **화면 이동**: 
  - 우클릭 + 드래그
  - 스페이스바 + 드래그
  - 미니맵 클릭 또는 드래그
- **뷰 리셋**: 줌 컨트롤의 **"⌂"** 버튼으로 전체 트랙 보기

### 미니맵
- 오른쪽 상단에 전체 트랙 미니맵 표시
- 녹색 사각형으로 현재 보이는 영역 표시
- 미니맵 클릭 또는 드래그로 화면 이동

### 데이터 저장
- 모든 트랙 데이터는 브라우저의 localStorage에 자동 저장
- 새로고침해도 데이터 유지
- 트랙 정보, 포인트, 체크포인트, 줌/팬 상태 모두 저장

### 트랙 로드
- **"트랙 로드"** 버튼으로 기존 트랙(basic-circuit, monza) 불러오기
- 불러온 트랙을 기반으로 편집 가능

### 코드 생성
- 트랙 정보가 변경될 때마다 코드 자동 업데이트
- **"코드 복사"** 버튼으로 클립보드에 복사
- 생성된 코드는 클라이언트와 서버 양쪽에서 사용 가능한 형식

---

## 주의사항

### 필수 항목
- ✅ 최소 2개 이상의 포인트 필요
- ✅ 트랙 ID는 고유해야 함 (기존 트랙과 중복 불가)
- ✅ 체크포인트는 순서대로 배치해야 함
- ✅ 서버 설정과 클라이언트 설정이 일치해야 함

### 권장 사항
- 📌 트랙은 닫힌 경로(시작점과 끝점이 연결)로 만들기
- 📌 포인트는 적절한 간격으로 배치 (너무 많으면 성능 저하)
- 📌 체크포인트는 각 코너나 주요 지점에 배치
- 📌 스폰 위치는 시작 라인 근처에 여러 개 배치

### 트랙 폭
- 모든 트랙에서 트랙 폭은 **120 픽셀**로 통일되어 있습니다.
- 트랙 폭을 변경하면 서버와 클라이언트 양쪽에서 일치시켜야 합니다.

---

## 문제 해결

### 트랙이 게임에 나타나지 않을 때
1. 브라우저 콘솔에서 에러 확인
2. 트랙 파일이 올바른 경로에 있는지 확인
3. `track.js`에 트랙이 등록되었는지 확인
4. HTML에서 트랙 파일이 로드되는지 확인

### 서버에서 트랙을 인식하지 못할 때
1. `track-configs.ts`에 서버 설정이 추가되었는지 확인
2. `trackServerConfigs` Map에 트랙이 등록되었는지 확인
3. 서버를 재시작했는지 확인

### 체크포인트가 작동하지 않을 때
1. 체크포인트가 순서대로 배치되었는지 확인
2. `checkCheckpoint` 함수가 올바르게 구현되었는지 확인
3. 체크포인트 범위(radius)가 적절한지 확인

---

## 예제: 간단한 원형 트랙 만들기

### 1. 트랙 정보 입력
- 트랙 이름: "원형 트랙"
- 트랙 ID: `circular-track`
- 맵 너비: 3000
- 맵 높이: 3000
- 트랙 폭: 120

### 2. 포인트 추가
원형 트랙을 만들기 위해 8개의 포인트를 원형으로 배치:
- (1500, 500) - 상단
- (2000, 800) - 우상단
- (2500, 1500) - 우측
- (2000, 2200) - 우하단
- (1500, 2500) - 하단
- (1000, 2200) - 좌하단
- (500, 1500) - 좌측
- (1000, 800) - 좌상단

모든 포인트를 **곡선** 타입으로 설정

### 3. 체크포인트 추가
- (1500, 500) - 시작점
- (2500, 1500) - 우측
- (1500, 2500) - 하단
- (500, 1500) - 좌측

### 4. 코드 생성 및 적용
위의 단계를 따라 게임에 적용

---

## 추가 리소스

- 기존 트랙 파일 참고:
  - `public/js/tracks/basic-circuit.js`
  - `public/js/tracks/monza.js`
- 서버 설정 참고:
  - `src/game/tracks/track-configs.ts`

---

## 업데이트 이력

- 2024: 트랙 에디터 초기 버전
- 체크포인트 기능 추가
- 미니맵 드래그 기능 추가
- 마우스 커서 기준 줌 기능 추가

---

**문의사항이나 문제가 있으면 개발팀에 문의하세요.**

